{
  "name": "daily.news",
  "author": "Thomas Trainset",
  "private": true,
  "scripts": {
    "format": "prettier --write packages/**/**/*.{ts,tsx,css,js}"
  },
  "devDependencies": {
    "lerna": "^4.0.0",
    "prettier": "^2.6.0"
  },
  "workspaces": {
    "packages": [
      "packages/*"
    ]
  }
}
